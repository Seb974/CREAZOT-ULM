import { ConversationThreadList } from "./ConversationThreadList";
import { ConversationThreadShow } from "./ConversationThreadShow";

const conversationThreadResourceProps = {
  list: ConversationThreadList,
  show: ConversationThreadShow,
  hasShow: true,
};

export default conversationThreadResourceProps;
